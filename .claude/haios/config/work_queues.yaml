# generated: 2026-01-15
# System Auto: last updated on: 2026-02-17T21:55:00
# Work Queue Configuration (E2-290, INV-064 design)
# Provides typed queues with cycle-locking for structured work execution
# Updated: S393 — reconfigured for E2.8 Agent UX (4 arcs, pending decomposition)
version: "1.1"

# Queue type definitions
queue_types:
  fifo:
    ordering: "creation_date ASC"
  priority:
    ordering: "priority DESC, creation_date ASC"
  batch:
    phases: [plan_all, review, implement_all, validate_all]
  chapter_aligned:
    ordering: "chapter_priority DESC, priority DESC"

# Active queues — E2.8 Agent UX
# Note: Arcs decomposed in S393 EPOCH.md but ARC.md files not yet created.
# Queue items will be populated as work items are created for each arc.
queues:
  # Default queue - catch-all for E2.8 work
  default:
    type: priority
    items: auto  # Populated from get_ready() excluding named queues
    allowed_cycles: [implementation-cycle, investigation-cycle, work-creation-cycle]

# Parked items — out of E2.8 scope
parked:
  items:
    - WORK-071  # Pre-Decomposition Review Gate (governance, E2.9)
    - WORK-102  # Session/Process Review Ceremonies (governance, E2.9)
  rationale: "S393: Governance-themed items deferred to E2.9. WORK-101 unparked for E2.8 CH-058."

# Policies
policies:
  assignment:
    default_queue: default  # E2.8: single queue until arc work items created
    auto_batch_threshold: 3
  cross_arc:
    allowed: true  # E2.8: arcs are independent
  cycle_enforcement:
    strict: true

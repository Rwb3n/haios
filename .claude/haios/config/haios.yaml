# generated: 2026-01-03
# System Auto: last updated on: 2026-02-03T20:01:50
# HAIOS System Configuration
# Consolidates: governance-toggles.yaml, routing-thresholds.yaml
# Version: 2.5 (Session 295 - Independent Lifecycles)

# Plugin manifest (for portable plugin spec)
manifest:
  name: haios
  version: "2.6"
  description: "Hybrid AI Operating System - Agent UX"

# Current epoch context (ADR-042: Arc > Chapter in hierarchy)
epoch:
  current: E2.6
  subversion: "2.6"  # Foundations
  epoch_file: ".claude/haios/epochs/E2_6/EPOCH.md"
  architecture_dir: ".claude/haios/epochs/E2_6/architecture"
  arcs_dir: ".claude/haios/epochs/E2_6/arcs"
  observations_dir: ".claude/haios/epochs/E2_6/observations"
  # Prior epoch (for reference)
  prior_epoch: ".claude/haios/epochs/E2_5/EPOCH.md"
  # Foundational architecture (MUST load) - carries forward
  foundational:
    - S20-pressure-dynamics.md
    - S21-cognitive-notation.md
    - S22-skill-patterns.md
    - S27-breath-model.md  # E2.4 discovery, foundational
  # Active arcs (E2.6 - Foundations, pending arc decomposition)
  active_arcs: [discoverability, referenceability, traceability, observability]
  # Epoch sequence (planned S365)
  sequence:
    - id: E2.6
      name: Foundations
      theme: "traceability, referenceability, discoverability, observability"
    - id: E2.7
      name: Composability
      theme: "flat metadata, engine functions, recipe rationalization"
    - id: E2.8
      name: Agent UX
      theme: "agents spend tokens on work, not bookkeeping"
    - id: E2.9
      name: Governance
      theme: "proportional governance, review ceremonies, scope management"
    - id: E3
      name: TBD
      theme: "determined by investigation — SDK? Autonomy? Product?"
    - id: E4
      name: Cognitive Memory
      theme: "three-paradigm memory, FORESIGHT, autonomous operation"

# Governance toggles (from governance-toggles.yaml)
toggles:
  block_powershell: true  # PowerShell through bash mangles $_ variables
  ceremony_contract_enforcement: warn  # warn|block (WORK-113)
  ceremony_context_enforcement: warn  # warn|block (CH-012: state change boundary)
  # block_sql: true       # Future: migrate from hardcoded

# Routing thresholds (from routing-thresholds.yaml)
thresholds:
  observation_pending:
    enabled: true
    max_count: 10
    divert_to: observation-triage-cycle
    escape_priorities: [critical]

# Path constants (single source of truth - WORK-080)
paths:
  # Work item paths
  work_dir: "docs/work"
  work_active: "docs/work/active"
  work_archive: "docs/work/archive"
  work_item: "docs/work/active/{id}/WORK.md"
  work_plan: "docs/work/active/{id}/plans/PLAN.md"
  work_queues: ".claude/haios/config/work_queues.yaml"

  # Document paths
  checkpoints: "docs/checkpoints"
  reports: "docs/reports"
  adr: "docs/ADR"
  specs: "docs/specs"

  # Plugin paths
  templates: ".claude/templates"
  skills: ".claude/skills"
  commands: ".claude/commands"
  agents: ".claude/agents"
  hooks: ".claude/hooks"

  # Schema registry (WORK-147: central schema location)
  schemas: ".claude/haios/schemas"

  # Config paths
  haios_config: ".claude/haios/config"
  manifesto: ".claude/haios/manifesto"
  status: ".claude/haios-status.json"
  status_slim: ".claude/haios-status-slim.json"

  # Event log paths (WORK-127: single canonical location)
  governance_events: ".claude/haios/governance-events.jsonl"

# Context loading configuration (WORK-008)
# Role-based loader dispatch per L4 principles
context:
  roles:
    main:
      loaders: [identity]
      description: "Main agent - philosophical grounding"
    builder:
      loaders: [identity]
      description: "Builder agent - identity + work context"
    validator:
      loaders: [identity]
      description: "Validator agent - identity + requirements"
  loader_registry:
    identity:
      module: identity_loader
      class: IdentityLoader

# Agent configurations (E2-072: Phase-as-Subagent pattern)
agents:
  critique:
    description: "Pre-implementation assumption surfacing"
    framework: assumption_surfacing  # Default framework
    frameworks_dir: ".claude/haios/config/critique_frameworks/"
    integration:
      gate: implementation-cycle.PLAN.Gate1
      trigger: pre-DO
    context_loading:
      - manifesto/L4/agent_user_requirements.md
      - work_item
      - plan
